[build]
  command = "next build"
  publish = ".next"
ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF :!app :!supabase"

[build.environment]
  NODE_VERSION = "18.20.3"

[[plugins]]
  package = "@netlify/plugin-nextjs"
